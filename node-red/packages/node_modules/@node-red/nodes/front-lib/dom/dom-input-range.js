function initInputRangeControl(controlObjectArray){
    for(let pair of controlObjectArray){
        const {input, view} = pair;
        const viewDom = $(view)
        const v = $(input)
        v.change(function (){
            viewDom.text(v.val())
        });
        viewDom.text(v.val())
    }
}
